# Database Configuration
# Format: postgresql://USERNAME:PASSWORD@HOST:PORT/DATABASE_NAME?schema=SCHEMA_NAME
# 
# Default PostgreSQL setup:
# - Username: postgres (default superuser)
# - Password: (the password you set during PostgreSQL installation)
# - Host: localhost
# - Port: 5432 (default PostgreSQL port)
# - Database: todo_db (you need to create this database first)
# - Schema: public (default schema)
#
# Example:
# DATABASE_URL="postgresql://postgres:mypassword123@localhost:5432/todo_db?schema=public"
DATABASE_URL="postgresql://postgres:YOUR_PASSWORD_HERE@localhost:5432/todo_db?schema=public"

# Server Configuration
PORT=3001

# JWT Secret (change this in production!)
JWT_SECRET=your-secret-key-change-in-production

# Instructions:
# 1. Install PostgreSQL (see POSTGRESQL_SETUP.md in the root folder)
# 2. Create a database named 'todo_db' using pgAdmin or psql
# 3. Copy this file to .env: copy env.template .env
# 4. Replace YOUR_PASSWORD_HERE with your actual PostgreSQL password
# 5. Save the file

